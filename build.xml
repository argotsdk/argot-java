<!--
 Copyright 2003-2005 (c) Live Media Pty Ltd. <argot@einet.com.au> 
 
 This software is licensed under the Argot Public License 
 which may be found in the file LICENSE distributed 
 with this software.
 
 More information about this license can be found at
 http://www.einet.com.au/License
  
 The Developer of this software is Live Media Pty Ltd,
 PO Box 4591, Melbourne 3001, Australia.  The license is subject 
 to the law of Victoria, Australia, and subject to exclusive 
 jurisdiction of the Victorian courts.
-->

<project name="argot" default="release" basedir=".">

  <description>Argot build</description>
  
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="bin" location="bin"/>
  <property name="dist"  location="dist"/>
  <property name="lib" location="lib"/>
  <property name="release" location="release"/>

  <target name="init">
    <tstamp/>
    <mkdir dir="${bin}"/>
  	<mkdir dir="${lib}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source">
                
	<path id="classpath">
		<fileset dir="${lib}">
			<include name="**/*.jar" />
		</fileset>
    </path>    
    <javac srcdir="${src}" destdir="${bin}" 
    	classpathref="classpath" includes="**/*.java" 
    	debug="on" optimize="true" deprecation="true" />    	
  </target>

  <target name="release" depends="compile"
        description="generate the distribution release" >

    <!-- Put everything in ${build} into the jar file -->
    <copy file="LICENSE.TXT" tofile="${bin}/LICENSE"/>
    
    <mkdir dir="${release}/lib"/>
    <jar jarfile="${release}/lib/argot-1.2.0.jar" basedir="${bin}"/>
    <copy file="LICENSE.TXT" tofile="${release}/LICENSE"/>    
    <copy file="README.TXT" tofile="${release}/README"/>
    
    <mkdir dir="${release}/argot"/>
    <copy file="argot/README.TXT" tofile="${release}/argot/README"/>
    <copy file="argot/common.argot" tofile="${release}/argot/common.argot"/>
    <copy file="argot/meta.argot" tofile="${release}/argot/meta.argot"/>
    
    <mkdir dir="${release}/doc"/>  	
    <copy file="../doc/ArgotProgrammersGuide.pdf" tofile="${release}/doc/ArgotProgrammersGuide.pdf"/>
  	
  	<mkdir dir="${release}/examples"/>
  	<copy todir="${release}/examples">
  		<fileset dir="examples"/>
  	</copy>
  	
  	<mkdir dir="${release}/src"/>
  	<copy todir="${release}/src">
  		<fileset dir="src"/>
  	</copy>
  	
  	<mkdir dir="${release}/test"/>
  	<copy todir="${release}/test">
  		<fileset dir="test"/>
  	</copy>  	
	
    <!-- zip up the release -->
    <zip basedir="${release}" destfile="${lib}/ArgotJava-1.2.0.zip"/>
  </target>

  <target name="clean" description="clean up" >
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
</project>
